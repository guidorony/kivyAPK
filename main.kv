#:import random random


<SnakePart>:
    size: 40, 40
    canvas.before:
        Color:
            rgb: random.random(),random.random(),random.random()
        Rectangle:
            size: self.size
            pos: self.pos


GameScreen:
    canvas.before:
        Rectangle:
            source: 'fondo.png'
            size: self.size
            pos: self.pos
    Widget:
        size: 40, 40
        pos: 120, 120
        id: food
        canvas.before:
            Color:
                rgb: random.random(),random.random(),random.random()
            Rectangle:
                size: self.size
                pos: self.pos
